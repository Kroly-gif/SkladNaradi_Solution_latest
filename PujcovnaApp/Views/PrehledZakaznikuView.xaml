<UserControl x:Class="PujcovnaApp.Views.PrehledZakaznikuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="TABULKA ZÁKAZNÍKŮ" FontSize="24" FontWeight="Bold" Foreground="#D4AC0D" Margin="0,0,0,10" HorizontalAlignment="Center"/>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding SeznamZakazniku}"
                  SelectedItem="{Binding VybranyZakaznik}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  ColumnHeaderStyle="{StaticResource GoldHeaderStyle}"
                  BorderBrush="#F1C40F" BorderThickness="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True" Width="40"/>
                <DataGridTextColumn Header="Jméno" Binding="{Binding Jmeno}" Width="100"/>
                <DataGridTextColumn Header="Příjmení" Binding="{Binding Prijmeni}" Width="100"/>
                <DataGridTextColumn Header="Tel. číslo" Binding="{Binding Telefon}" Width="100"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="150"/>
                <DataGridTextColumn Header="Adresa" Binding="{Binding Adresa}" Width="150"/>
                <DataGridTextColumn Header="Organizace" Binding="{Binding Organizace}" Width="100"/>
                <DataGridTextColumn Header="Poznámka" Binding="{Binding Poznamka}" Width="*"/>
                <DataGridCheckBoxColumn Header="BAN" Binding="{Binding Ban}" Width="50"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <TextBlock Text="Hledat:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Text="{Binding HledanyText, UpdateSourceTrigger=PropertyChanged}" Width="150" Margin="0,5,5,5" VerticalContentAlignment="Center"/>
            <Button Content="Filtrovat" Command="{Binding FiltrovatCommand}" Width="100" Background="#DDD"/>

            <Border Width="20"/>

            <Button Content="Přidat zákazníka" Command="{Binding PridatCommand}" Width="130"/>
            <Button Content="Smazat" Command="{Binding SmazatCommand}" Width="100" Background="#FFCDD2"/>
            <Button Content="Uložit změny" Command="{Binding UlozitCommand}" Width="120" Background="#C8E6C9" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</UserControl>