<UserControl x:Class="PujcovnaApp.Views.PrehledNaradiView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="TABULKA NÁŘADÍ" FontSize="24" FontWeight="Bold" Foreground="#8E44AD" Margin="0,0,0,10" HorizontalAlignment="Center"/>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding SeznamNaradi}"
                  SelectedItem="{Binding VybraneNaradi}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  ColumnHeaderStyle="{StaticResource PurpleHeaderStyle}" 
                  BorderBrush="#9B59B6" BorderThickness="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True" Width="40"/>
                <DataGridTextColumn Header="Nářadí" Binding="{Binding Nazev}" Width="*"/>

                <DataGridTextColumn Header="Cena/Den" Binding="{Binding CenaZaDen, StringFormat={}{0:N0} Kč}" Width="90" FontWeight="Bold"/>

                <DataGridTextColumn Header="Výkon" Binding="{Binding Vykon}" Width="80"/>

                <DataGridCheckBoxColumn Header="Dostupnost" Binding="{Binding Dostupnost}" Width="80"/>

                <DataGridTextColumn Header="Umístění" Binding="{Binding Umisteni}" Width="100"/>
                <DataGridTextColumn Header="Hmotnost" Binding="{Binding Hmotnost}" Width="80"/>
                <DataGridTextColumn Header="Popis" Binding="{Binding Popis}" Width="150"/>
                <DataGridTextColumn Header="Poznámka" Binding="{Binding Poznamka}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <TextBlock Text="Hledat:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Text="{Binding HledanyText, UpdateSourceTrigger=PropertyChanged}" Width="150" Margin="0,5,5,5" VerticalContentAlignment="Center"/>

            <Button Content="Filtrovat" Command="{Binding FiltrovatCommand}" Width="100" Background="#DDD"/>

            <Border Width="20"/>

            <Button Content="Přidat nářadí" Command="{Binding PridatCommand}" Width="120"/>
            <Button Content="Smazat" Command="{Binding SmazatCommand}" Width="100" Background="#FFCDD2"/>
            <Button Content="Uložit změny" Command="{Binding UlozitCommand}" Width="120" Background="#C8E6C9" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</UserControl>