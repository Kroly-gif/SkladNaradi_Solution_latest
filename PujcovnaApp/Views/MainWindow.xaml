<Window x:Class="PujcovnaApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:PujcovnaApp.ViewModels"
        xmlns:views="clr-namespace:PujcovnaApp.Views"
        mc:Ignorable="d"
        Title="Půjčovna Nářadí" Height="650" Width="1100">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:PrehledNaradiVM}">
            <views:PrehledNaradiView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:PrehledZakaznikuVM}">
            <views:PrehledZakaznikuView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:VypujckyVM}">
            <views:VypujckyView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#EEE" Padding="10">
            <StackPanel Orientation="Horizontal">
                <Button Content="Nářadí" Command="{Binding OtevritNaradiCommand}" Width="100" Margin="5"/>
                <Button Content="Zákazníci" Command="{Binding OtevritZakaznikyCommand}" Width="100" Margin="5"/>
                <Button Content="Výpůjčky" Command="{Binding OtevritVypujckyCommand}" Width="100" Margin="5" FontWeight="Bold"/>
            </StackPanel>
        </Border>

        <ContentControl Grid.Row="1" Content="{Binding AktualniPohled}"/>
    </Grid>
</Window>